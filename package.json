{
  "name": "plataforma-cidade-imperial",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma da Loja Cidade Imperial — full stack (PostgreSQL + API Node.js + web) em um único repositório, sem Docker",
  "scripts": {
    "start": "node scripts/start.mjs",
    "build:web": "npm --prefix web install && npm --prefix web run build",
    "start:server": "node server/src/index.js",
    "migrate": "node server/src/migrate.js",
    "docker": "docker compose up --build",
    "docker:stop": "docker compose down"
  }
}
